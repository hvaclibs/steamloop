(installation)=

# Installation

The package is published on [PyPI](https://pypi.org/project/steamloop/) and can be installed with `pip` (or any equivalent):

```bash
pip install steamloop
```

Next, see the {ref}`section about usage <usage>` to see how to use it.
